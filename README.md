# amazon_mail_20191106

```python
# driver.mail.py
mail(driver, seller_id, shop_name)
> seller_idから出品者のURLを生成し、そこからメールを送信するまでを行う。
>
> requests モジュールでのPOSTではなく、seleniumでブラウザ操作を再現している
>> POSTでもできるようにAPIエンドポイントからPOSTしてみましたが、Cookieの関係等でうまくいきませんでした。
>> 送信制限等で早く送信することに意味はないのでそちらは実装まではしていません。

# main.py
こちらがメインのファイルとなります。（途中で開発終了したので動きますが、汚いです。。。）
csvファイルを指定してあげると、そこからURL,ショップ名を順に取得します。
それをもとに順々にメールを送るという感じです。
```
